import { MagneticDirective } from './magnetic.directive';

describe('MagneticDirective', () => {
  it('should create an instance', () => {
    const directive = new MagneticDirective();
    expect(directive).toBeTruthy();
  });
});
